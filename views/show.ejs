<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <a href="http://localhost:8080/user/new">Create new User</a>
    <table border="1">
        <tr>
            <th>ID</th>
            <th>username</th>
            <th>email</th>
            <th>Edit</th>
        </tr>
        <% for(user of results) { %>
            <tr>
                <td><%= user.id%></td>
                <td><%= user.username%></td>
                <td><%= user.email%></td>
                <td>
                <form method="get" action="/user/<%= user.id%>/edit">
                    <button>Edit user</button>
                </form>
                </td>
                <td>
                    <form method="post" action="/user/<%= user.username%>?_method=DELETE">
                        <button>Delete</button>
                    </form>
                </td>
            </tr>
        <%}%>
    </table>
    
</body>
</html>